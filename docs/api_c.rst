.. Generated by wcwidth code generation (bin/update-docs.py); do not edit.

.. _c-api:

C11 Public API
==============

This library is released together with the Python `wcwidth` package: any change
to C11 ships with a PyPI release, and the same SEMVER_ rules apply.  The C11
API is stable within a major version; function signatures do not change until
a new major release.

wcwidth.h
---------

Unicode character display width: wcwidth, wcswidth, wcstwidth.

.. c:function:: int wcwidth_u32(uint32_t codepoint, int ambiguous_width)


   Return the display width of a single Unicode codepoint.

   Returns:
   1 or 2  -- display cells occupied
   0       -- zero-width codepoint (combining marks, ZWJ, etc.)
   -1       -- non-printable control character

   :param ambiguous_width: width for East Asian Ambiguous (A) characters. 1 = narrow (default), 2 = wide (CJK context).

.. c:function:: int wcswidth_u32(const uint32_t *codepoints, size_t n, int ambiguous_width)


   Return the display width of a string of Unicode codepoints.

   This function handles grapheme clusters (ZWJ, VS16, virama, RI pairs, etc.).

   :param n: number of codepoints in the array.

.. c:function:: int wcswidth_u8(const char *utf8, size_t n, int ambiguous_width)


   UTF-8 variant of wcswidth_u32().

   measure as zero-width.  There is no "compute the length for me" sentinel:
   :param n: number of UTF-8 bytes to process.  Exactly \*n\* bytes are read; the text is never treated as NUL-terminated, so embedded NULs are permitted and pass strlen(utf8) explicitly if that is what you mean.

.. c:function:: int wcstwidth_u32(const uint32_t *codepoints, size_t n, int ambiguous_width, const char *term_program)


   Terminal-aware variant of wcswidth_u32().

   :param term_program: canonical terminal name for override tables (e.g. "kitty", "xterm", "ghostty"). Use NULL for no terminal overrides.

.. c:function:: int wcstwidth_u8(const char *utf8, size_t n, int ambiguous_width, const char *term_program)


   Terminal-aware variant of wcswidth_u8().



width.h
-------

Main entry-points for string display width: width_u32 / width_u8.

.. c:enum:: wcwidth_control_mode_t

   How width_u32() and width_u8() treat control characters and sequences.

   .. c:enumerator:: WCWIDTH_PARSE

      track horizontal cursor movement

   .. c:enumerator:: WCWIDTH_STRICT

      raise errors on indeterminate sequences

   .. c:enumerator:: WCWIDTH_IGNORE

      strip all control codes and sequences

.. c:enum:: wcwidth_error_t

   Error codes written to the \*error out-param of width_u32/width_u8 (and the
   string-measuring functions of clip.h/align.h).  Distinct codes let callers
   distinguish the failure cause.

   .. c:enumerator:: WCWIDTH_ERROR_NONE

      no error

   .. c:enumerator:: WCWIDTH_ERROR_INDETERMINATE

      indeterminate terminal sequence

   .. c:enumerator:: WCWIDTH_ERROR_ILLEGAL_CTRL

      illegal C0/C1 control character

   .. c:enumerator:: WCWIDTH_ERROR_VERTICAL_CTRL

      vertical movement control character

   .. c:enumerator:: WCWIDTH_ERROR_CURSOR_LEFT_EXCEED

      CUB/backspace moves before column 0

   .. c:enumerator:: WCWIDTH_ERROR_CURSOR_LEFT_ABSOLUTE

      HPA to an indeterminate column

   .. c:enumerator:: WCWIDTH_ERROR_HORIZONTAL_MOVEMENT

      CR with indeterminate starting column

.. c:struct:: wcwidth_width_opts_t

   Measurement options for width_u32() and width_u8().

   .. c:member:: int tabsize

      tab stop width (default 8)

   .. c:member:: int ambiguous_width

      1 or 2

   .. c:member:: const char *term_program

      NULL or terminal name

.. c:var:: const wcwidth_width_opts_t WCWIDTH_WIDTH_OPTS_DEFAULT

   Default options for width_u32() and width_u8().

.. c:function:: int width_u32(const uint32_t *codepoints, size_t n, wcwidth_control_mode_t mode, const wcwidth_width_opts_t *opts, int *error)


   Measure the visible width of text, including terminal control sequences
   such as colors, bold, tabstops, cursor movement, and OSC 66 Text Sizing.  width_u32() encodes its codepoints to UTF-8 and
   measures as width_u8().

   Returns the width in display cells, or -1 on error.

   :param mode: how control characters and sequences are treated (WCWIDTH_PARSE, WCWIDTH_STRICT, or WCWIDTH_IGNORE).
   :param opts: measurement options, or NULL for defaults.
   :param error: written with a wcwidth_error_t code when mode is WCWIDTH_STRICT and the input is indeterminate; may be NULL.

.. c:function:: int width_u8(const char *utf8, size_t n, wcwidth_control_mode_t mode, const wcwidth_width_opts_t *opts, int *error)

   UTF-8 variant of width_u32().


textwrap.h
----------

Text wrapping with ANSI-aware display width measurement.

.. c:struct:: wcwidth_wrap_opts_t

   Options for wrap_u8() and wrap_u8_text().

   .. c:member:: int width

      max line width in display cells

   .. c:member:: wcwidth_control_mode_t control_codes

   .. c:member:: int tabsize

      tab stop width; <= 0 passes tabs through as-is

   .. c:member:: int ambiguous_width

   .. c:member:: const char *term_program

   .. c:member:: bool expand_tabs

   .. c:member:: bool replace_whitespace

   .. c:member:: bool break_long_words

   .. c:member:: bool drop_whitespace

   .. c:member:: int max_lines

      0 = no limit

   .. c:member:: const char *initial_indent

   .. c:member:: const char *subsequent_indent

   .. c:member:: const char *placeholder

      for truncation, default " [...]"

.. c:var:: const wcwidth_wrap_opts_t WCWIDTH_WRAP_OPTS_DEFAULT

   Default options for wrap().

.. c:function:: int wrap_u8(const char *text, size_t text_len, const wcwidth_wrap_opts_t *opts, char **out, size_t *out_len)


   Wrap UTF-8 text into lines.

   Treats all whitespace (including '\n') as inter-word spaces and collapses
   it.  Use wrap_u8_text() to preserve input newlines as paragraph breaks.

   Returns 0 on success, -1 on allocation error, and -2 when the placeholder
   does not fit within the given width (max_lines truncation).
   On success, \*out points to a single malloc'd buffer containing all lines,
   separated by '\n' (no trailing newline).  \*out_len is the total byte length.
   The caller does a single free(\*out) to release memory.


.. c:function:: int wcwidth_wrap_lines_u8(const char *text, size_t text_len, const wcwidth_wrap_opts_t *opts, char **out, size_t *out_len, size_t **offsets, size_t *offset_count)


   Like wrap_u8(), but also reports the start offset of each line in the
   output buffer.  Lines are joined by '\n', so a line's byte length is
   \*offsets\*[i+1] - \*offsets\*[i] - 1 (or \*out_len\* - \*offsets\*[i] for the
   last), and a line may itself contain '\n' when the placeholder does.

   Returns the same codes as wrap_u8().  On success, \*offsets\* is a malloc'd
   array of \*offset_count\* start offsets; the caller does free(\*offsets) in
   addition to free(\*out).  Empty output yields \*offset_count\* == 0.


.. c:function:: int wrap_u8_text(const char *text, size_t text_len, const wcwidth_wrap_opts_t *opts, char **out, size_t *out_len)


   Wrap UTF-8 text preserving input newlines as paragraph breaks.

   Splits text on '\n', wraps each non-empty line individually with wrap_u8(),
   and preserves empty or whitespace-only lines as paragraph breaks.  Paragraph
   breaks appear as "\n\n" in the output.

   Returns 0 on success, -1 on allocation error.
   On success, \*out points to a single malloc'd buffer containing all lines,
   separated by '\n' (no trailing newline).  \*out_len is the total byte length.
   The caller does a single free(\*out) to release memory.


.. c:function:: int wrap_u32(const uint32_t *codepoints, size_t n, const wcwidth_wrap_opts_t *opts, uint32_t **out, size_t *out_len)


   Codepoint-array variant of wrap_u8(): encodes the codepoints to UTF-8,
   wraps, and decodes the result back to a codepoint array.  The output is a
   malloc'd array of \*out_len\* codepoints and the caller does a single free()
   of it, exactly as wrap_u8() returns UTF-8 bytes.

   Returns 0 on success, -1 on allocation error.


.. c:function:: int wrap_u32_text(const uint32_t *codepoints, size_t n, const wcwidth_wrap_opts_t *opts, uint32_t **out, size_t *out_len)


   Codepoint-array variant of wrap_u8_text().



align.h
-------

Text alignment: ljust, rjust, center.

.. c:function:: char *ljust_u8(const char *text, size_t text_len, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)


   Left/right/center justify text to a display width.

   Each returns a malloc'd NUL-terminated string on success, or NULL on
   error.  When NULL is returned and \*error is set to a nonzero
   wcwidth_error_t, the failure is a WCWIDTH_STRICT violation;
   otherwise it is an allocation failure.  \*error may be NULL, in which
   case errors are signaled only by the NULL return.  On success, \*out_len
   receives the byte length (excluding NUL) when non-NULL; the caller must
   free() the result.

   The control_codes, ambiguous_width, and term_program arguments are as for
   width_u8().

   :param text: UTF-8 input, NOT NUL-terminated.
   :param text_len: byte length of text.
   :param dest_width: target display width in cells.
   :param fillchar: UTF-8 padding bytes (repeated once per padding cell).
   :param fillchar_len: byte length of fillchar.
   :param out_len: output: byte length of result (excluding NUL).
   :param error: output: wcwidth_error_t, WCWIDTH_ERROR_NONE on success.

.. c:function:: char *rjust_u8(const char *text, size_t text_len, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)

.. c:function:: char *center_u8(const char *text, size_t text_len, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)

.. c:function:: uint32_t *ljust_u32(const uint32_t *codepoints, size_t n, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)


   Codepoint-array variants of ljust_u8()/rjust_u8()/center_u8(): encode the
   codepoints to UTF-8, justify, and decode the result back to a codepoint
   array.  The returned array is \*out_len\* codepoints and the caller does a
   single free() of it.  Error and ownership semantics are as for the _u8()
   forms: on NULL return, \*error\* is a nonzero wcwidth_error_t for a
   WCWIDTH_STRICT violation, or WCWIDTH_ERROR_NONE for an allocation failure.
   \*fillchar\* stays UTF-8 bytes.


.. c:function:: uint32_t *rjust_u32(const uint32_t *codepoints, size_t n, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)

.. c:function:: uint32_t *center_u32(const uint32_t *codepoints, size_t n, size_t dest_width, const char *fillchar, size_t fillchar_len, wcwidth_control_mode_t control_codes, int ambiguous_width, const char *term_program, size_t *out_len, int *error)


clip.h
------

Clip text to a visible column range [v_start, v_end).

.. c:function:: char *clip_u8(const char *text, size_t text_len, size_t v_start, size_t v_end, wcwidth_control_mode_t control_codes, int tabsize, int ambiguous_width, const char *term_program, bool propagate_sgr, const char *fillchar, size_t fillchar_len, size_t *out_len, int *error)


   Clip text to the visible column range [v_start, v_end).

   Returns a malloc'd string on success, NULL on error.  When NULL is
   returned and \*error is set to a nonzero wcwidth_error_t (from width.h),
   the failure is a WCWIDTH_STRICT violation; otherwise it is an
   allocation failure.  \*error is always written on return.
   On success, \*out_len receives the byte length of the result
   (excluding NUL terminator, which is always present).
   The caller must free the returned pointer with a single free() call.

   :param text: UTF-8 encoded input string.
   :param text_len: length of text in bytes (NOT NUL-terminated).
   :param v_start: starting column (inclusive, 0-indexed).
   :param v_end: ending column (exclusive).
   :param control_codes: how to handle control characters and sequences. WCWIDTH_STRICT raises on indeterminate sequences; cursor movement and OSC text sizing are not parsed.
   :param tabsize: tab stop width (0 = pass tabs through as-is).
   :param ambiguous_width: width for East Asian Ambiguous (A) characters (1 or 2).
   :param term_program: terminal name for override tables (NULL = none).
   :param propagate_sgr: if true, wrap result with SGR state at first visible char.
   :param fillchar: fill UTF-8 bytes for partially visible graphemes (display width 1).
   :param fillchar_len: byte length of fillchar.
   :param out_len: output: byte length of result (excluding NUL).
   :param error: output: wcwidth_error_t, WCWIDTH_ERROR_NONE on success.

.. c:function:: uint32_t *clip_u32(const uint32_t *codepoints, size_t n, size_t v_start, size_t v_end, wcwidth_control_mode_t control_codes, int tabsize, int ambiguous_width, const char *term_program, bool propagate_sgr, const char *fillchar, size_t fillchar_len, size_t *out_len, int *error)


   Codepoint-array variant of clip_u8(): encodes the codepoints to UTF-8,
   clips, and decodes the result back to a codepoint array.  The returned
   array is \*out_len\* codepoints and the caller does a single free() of it.
   Error and ownership semantics are as for clip_u8(): on NULL return, \*error\*
   is a nonzero wcwidth_error_t for a WCWIDTH_STRICT violation, or
   WCWIDTH_ERROR_NONE for an allocation failure.  \*fillchar\* stays UTF-8
   bytes.



escape.h
--------

Terminal escape sequence classification.

.. c:enum:: wcwidth_esc_type_t

   Types of escape sequences the parser can classify.

   .. c:enumerator:: WCWIDTH_ESC_NONE

      not an escape sequence

   .. c:enumerator:: WCWIDTH_ESC_UNRECOGNIZED

      lone ESC or malformed

   .. c:enumerator:: WCWIDTH_ESC_SGR

      CSI ... m (Select Graphic Rendition)

   .. c:enumerator:: WCWIDTH_ESC_CUF

      CSI n C (Cursor Forward)

   .. c:enumerator:: WCWIDTH_ESC_CUB

      CSI n D (Cursor Backward)

   .. c:enumerator:: WCWIDTH_ESC_HPA

      CSI n G (Horizontal Position Absolute)

   .. c:enumerator:: WCWIDTH_ESC_OSC66

      OSC 66;... (Text Sizing)

   .. c:enumerator:: WCWIDTH_ESC_INDETERMINATE

      clear, scroll, cursor up/down, etc.

   .. c:enumerator:: WCWIDTH_ESC_OTHER

      any other recognized zero-width sequence

.. c:struct:: wcwidth_esc_result_t

   Parsed parameters from a classified escape sequence.

   .. c:member:: wcwidth_esc_type_t type

   .. c:member:: const char *start

      pointer to start of sequence in original text

   .. c:member:: size_t length

      total length of sequence in bytes

   .. c:member:: const char *sgr_params

      For SGR: pointer to parameter string (after CSI, before 'm'), length

   .. c:member:: size_t sgr_params_len

   .. c:member:: int cursor_n

      For CUF/CUB/HPA: parsed numeric parameter (defaults to 1 if absent)

   .. c:member:: const char *ts_meta

      For OSC 66: pointers to meta, text, and terminator parts

   .. c:member:: size_t ts_meta_len

   .. c:member:: const char *ts_text

   .. c:member:: size_t ts_text_len

   .. c:member:: char ts_terminator

      '\x07' or '\x1b' (ST)

.. c:function:: bool wcwidth_escape_classify(const char *text, size_t text_len, size_t offset, wcwidth_esc_result_t *result)

   Classify the escape sequence starting at \*text[offset]\*.
   Returns false if no escape sequence is found (text[offset] != ESC).
   On success, fills \*result with parsed data and returns true.

   :param text: full input text
   :param text_len: total length of text
   :param offset: position of the ESC character
   :param result: output, filled on success

.. c:function:: size_t wcwidth_escape_strip(const char *text, size_t text_len, char *out, size_t out_cap, size_t *out_len)

   Strip all terminal escape sequences from text.
   OSC 66 inner display text is preserved as visible output.
   Writes result to \*out\* (caller-provided buffer).
   Returns the byte length of the stripped text, excluding the NUL terminator,
   so \*out\* must have capacity for the return value plus one.  Output was
   truncated when the return value is >= out_cap; pass an out_cap of 0 to
   measure without writing.

   :param out_cap: capacity of out buffer.
   :param out_len: filled with actual bytes written (excluding NUL terminator if any).

.. c:function:: uint32_t *wcwidth_escape_strip_u32(const uint32_t *codepoints, size_t n, size_t *out_len)


   Codepoint-array variant of wcwidth_escape_strip(): encodes the codepoints
   to UTF-8, strips, and decodes the result back to a codepoint array.  The
   returned array is \*out_len\* codepoints and the caller does a single free()
   of it; returns NULL on allocation failure.


.. c:type:: wcwidth_escape_iter_fn

   Callback for iterating over text segments.
   :param segment: text segment
   :param seg_len: length of segment
   :param is_escape: true if this segment is an escape sequence, false if visible text
   :param userdata: caller-provided pointer

.. c:function:: void wcwidth_escape_iter(const char *text, size_t text_len, wcwidth_escape_iter_fn fn, void *userdata)

   Iterate over text, calling \*fn\* for each segment (escape or visible text),
   without allocating.

.. c:function:: bool wcwidth_escape_has_cursor_movement(const char *text, size_t text_len)

   Check if text contains any horizontal cursor movement:
   BS (0x08), CR (0x0d), or CSI n C/D/G
   Returns true if found.



utf8.h
------

UTF-8 decoding and encoding.

.. c:function:: size_t wcwidth_utf8_decode_single(const char *s, size_t len, uint32_t *cp_out)


   Decode one UTF-8 codepoint from \*s\* (at most \*len\* bytes).  Returns the
   number of bytes consumed (1-4).  On error or truncation, \*cp_out\* is set to
   U+FFFD and the return value is the number of bytes to skip (1 for a bad
   leading byte, the remaining length for a truncated sequence, or 0 when
   \*len\* is 0).


.. c:function:: uint32_t *wcwidth_decode_u32(const char *utf8, size_t n, uint32_t *stack, size_t stack_cap, size_t *count)


   Decode \*utf8\* into a codepoint array.  The caller provides \*stack\* (up to
   \*stack_cap\* entries) as scratch space; when the result fits it is returned
   in \*stack\* (no allocation), otherwise a heap buffer is allocated.  The
   caller must free() the result only when it is not \*stack\*; freeing \*stack\*
   is undefined behavior.  Sets \*count\* and returns NULL on allocation
   failure.

   The result is not const, so the `if (p != stack) free(p);` release is a
   plain free() -- matching wcwidth_encode_u32() below.  Treat the contents as
   read-only; the pointer is non-const only so ownership can be released.


.. c:function:: uint32_t *wcwidth_decode_u32_heap(const char *utf8, size_t n, size_t *count)


   Decode \*utf8\* into a malloc'd codepoint array (one free()).  Returns the
   array and sets \*count\*, or NULL on allocation failure.


.. c:function:: char *wcwidth_encode_u32(const uint32_t *codepoints, size_t n, char *stack, size_t stack_cap, size_t *out_len)


   Encode \*codepoints\* (n entries) to UTF-8.  The caller provides \*stack\* (up
   to \*stack_cap\* bytes) as scratch space; when the result fits it is returned
   in \*stack\* (no allocation), otherwise a heap buffer is allocated.  The
   caller must free() the result only when it is not \*stack\*; freeing \*stack\*
   is undefined behavior.  Sets \*out_len\* and returns NULL on allocation
   failure.

   The result is not NUL-terminated.  Invalid codepoints (lone surrogates and
   values above U+10FFFF) are encoded as U+FFFD.



grapheme.h
----------

Grapheme cluster segmentation for UTF-8 text.

.. c:type:: wcwidth_grapheme_iter_t

   Opaque iterator for grapheme cluster segmentation.

.. c:function:: wcwidth_grapheme_iter_t *wcwidth_grapheme_iter_new(const char *utf8, size_t len)

   Initialize an iterator over grapheme clusters in a UTF-8 string.
   Returns NULL if allocation fails (malloc for iterator state).


.. c:function:: const char *wcwidth_grapheme_next(wcwidth_grapheme_iter_t *iter, size_t *out_len)

   Return the next grapheme cluster, or NULL when exhausted.
   \*out_len\* receives the byte length of the cluster.
   The returned pointer is into the original text buffer.


.. c:function:: void wcwidth_grapheme_iter_free(wcwidth_grapheme_iter_t *iter)

   Free the iterator.

.. c:function:: size_t wcwidth_grapheme_boundary_before(const char *utf8, size_t len, size_t pos)

   Find the grapheme cluster boundary immediately before \*pos\*.
   Returns the byte offset of the cluster start.



sgr.h
-----

SGR (Select Graphic Rendition) escape sequence state management.

.. c:macro:: WCWIDTH_SGR_COLOR_MAX

   Maximum number of color components stored (e.g. (38, 5, N) or (38, 2, R, G, B)).

   Defined as ``6``.

.. c:macro:: WCWIDTH_SGR_PROPAGATE_SPARE

   Spare capacity, in bytes, that each line passed to wcwidth_sgr_propagate
   must have beyond its NUL terminator: the carried-over SGR state can expand
   to this many bytes of prefix plus a reset suffix.

   Defined as ``256``.

.. c:struct:: wcwidth_sgr_state_t

   SGR state tracking: boolean attributes + foreground/background color.

   .. c:member:: bool bold

   .. c:member:: bool dim

   .. c:member:: bool italic

   .. c:member:: bool underline

   .. c:member:: bool blink

   .. c:member:: bool rapid_blink

   .. c:member:: bool inverse

   .. c:member:: bool hidden

   .. c:member:: bool strikethrough

   .. c:member:: bool double_underline

   .. c:member:: int fg[WCWIDTH_SGR_COLOR_MAX]

      Foreground color: first element is 38 or 0; 0 = default/unset.

   .. c:member:: int fg_len

   .. c:member:: int bg[WCWIDTH_SGR_COLOR_MAX]

      Background color: first element is 48 or 0; 0 = default/unset.

   .. c:member:: int bg_len

.. c:var:: const wcwidth_sgr_state_t WCWIDTH_SGR_STATE_DEFAULT

   Default state (no attributes, no colors).

.. c:function:: void wcwidth_sgr_update(wcwidth_sgr_state_t *state, const char *sgr_params, size_t sgr_params_len)

   Parse an SGR escape sequence and update \*state\*.
   :param sgr_params: the parameter string (between '[' and 'm'), e.g. "1;31;44"
   :param sgr_params_len: length of parameter string
   :param state: current state, updated in-place

.. c:function:: bool wcwidth_sgr_is_active(const wcwidth_sgr_state_t *state)

   Check if any attributes or colors are set (non-default).

.. c:function:: size_t wcwidth_sgr_to_escape(const wcwidth_sgr_state_t *state, char *out, size_t out_cap)

   Generate the minimal SGR escape sequence needed to restore \*state\* from reset.
   Writes to \*out\* (caller buffer). Returns bytes written (excluding NUL).
   out_cap must be at least WCWIDTH_SGR_PROPAGATE_SPARE bytes.


.. c:function:: int wcwidth_sgr_propagate(char **lines, const size_t *line_lens, size_t *out_lens, size_t nlines)

   Propagate SGR codes across an array of wrapped lines.
   Modifies lines in-place.  Each line is NUL-terminated and must have at
   least WCWIDTH_SGR_PROPAGATE_SPARE bytes of spare capacity beyond \*line_lens[i]\*.
   \*line_lens\* carries the actual byte length of each line (may include
   embedded NULs); \*out_lens\* receives the new byte length after SGR
   prefix/suffix insertion (may be NULL).
   Returns 0 on success, -1 if any buffer is too small.



text_sizing.h
-------------

OSC 66 Text Sizing protocol parsing and measurement.

.. c:struct:: wcwidth_ts_params_t

   Parsed OSC 66 Text Sizing parameters.

   .. c:member:: int scale

      repeat count, default 1

   .. c:member:: int width

      0 = auto-width from text, >0 = fixed width

   .. c:member:: int numerator

      fraction numerator (0 = don't care)

   .. c:member:: int denominator

      fraction denominator (0 = don't care)

   .. c:member:: int vertical_align

      0=top, 1=bottom, 2=center

   .. c:member:: int horizontal_align

      0=left, 1=right, 2=center

.. c:struct:: wcwidth_text_sizing_t

   A parsed OSC 66 Text Sizing sequence.

   .. c:member:: wcwidth_ts_params_t params

   .. c:member:: const char *text

      pointer into original buffer

   .. c:member:: size_t text_len

   .. c:member:: char terminator

      '\x07' or '\x1b'

.. c:function:: bool wcwidth_ts_parse_params(const char *meta, size_t meta_len, wcwidth_ts_params_t *params)

   Parse OSC 66 parameters from the colon-separated meta string.
   :param meta: parameter string, e.g. "s=2:w=3:n=1:d=2:v=1:h=1"
   :param meta_len: length of meta string.  Exactly meta_len bytes are read; \*meta\* need not be NUL-terminated. Returns true on success.

.. c:function:: void wcwidth_ts_from_esc(const wcwidth_esc_result_t *esc, wcwidth_text_sizing_t *ts)

   Build a text sizing struct from a classified OSC 66 escape result,
   applying default parameters.

.. c:function:: int wcwidth_ts_display_width(const wcwidth_text_sizing_t *ts, int ambiguous_width)

   Compute the display width of a text sizing sequence.
   :param ambiguous_width: 1 or 2 (see wcwidth.h) Uses wcswidth_u8 internally for width measurement.


terminal_override.h
-------------------

Terminal override lookup.

Shared by wcstwidth.c and width.c: resolves a terminal identifier to its
generated override table and looks up grapheme cluster overrides.

.. c:function:: const wcwidth_terminal_override_t *wcwidth_resolve_terminal(const char *term_program)


   Resolve a terminal identifier to its generated override table.

   \*term_program\* is a TERM_PROGRAM/TERM value, alias, or canonical terminal
   name (case-insensitive, surrounding whitespace ignored).  Returns NULL when
   it is NULL/empty or does not name a known terminal; in that case no
   overrides apply.


.. c:function:: int wcwidth_grapheme_override_lookup(const wcwidth_terminal_override_t *term, const uint32_t *cps, size_t len)


   Look up a grapheme cluster (a codepoint sequence) in \*term\*'s override
   table.  Returns the terminal-measured width, or -1 when the cluster is not
   overridden.


.. c:function:: size_t wcwidth_scan_zwj_cluster_end(const uint32_t *cp, size_t n, size_t start)


   Scan forward from \*start\* (a base character) to the end of a ZWJ grapheme
   cluster, following UAX #29 GB11 (ExtPict Extend\* ZWJ x ExtPict, chained).
   Returns the index just past the cluster.


.. c:function:: size_t wcwidth_scan_zwj_cluster_end_u8(const char *utf8, size_t n, size_t start)

   UTF-8 variant; \*start\* is a byte offset and the returned index is a byte offset.

.. c:function:: size_t wcwidth_decode_cluster(const char *utf8, size_t lo, size_t hi, uint32_t *cps, size_t cap)


   Decode the UTF-8 bytes in utf8[lo..hi) into \*cps\*, up to \*cap\* codepoints.
   Returns the number decoded; when the range holds more than \*cap\* codepoints
   the caller must treat the cluster as unmatched (no override key can be that
   long).



wcwidth_config.h
----------------

Library version, Unicode version, and UTF-8 configuration macros.

The version macros track the Python `wcwidth` package release: C11 and
Python ship together, so the same version number applies to both.

.. c:macro:: WCWIDTH_VERSION_MAJOR

   Defined as ``0``.

.. c:macro:: WCWIDTH_VERSION_MINOR

   Defined as ``9``.

.. c:macro:: WCWIDTH_VERSION_PATCH

   Defined as ``0``.

.. c:macro:: WCWIDTH_VERSION

   Defined as ``"0.9.0"``.

.. c:macro:: WCWIDTH_UNICODE_VERSION

   Unicode version the library tables were generated from.

   Defined as ``"17.0.0"``.

.. c:macro:: WCWIDTH_UTF8_MAX_BYTES

   Maximum number of bytes needed to decode a single UTF-8 codepoint.

   Defined as ``4``.

.. c:macro:: WCWIDTH_CTRL_WIDTH

   Return value indicating a non-printable control character.

   Defined as ``(-1)``.


table_types.h
-------------

Table data model: the interval type, the terminal-override record layouts,
and the binary search over them.

Hand-written.  The tables themselves -- every WCWIDTH_* interval array, the
terminal override and alias arrays, and their entry counts -- are declared
in tables.h, which update-tables.py generates.

.. c:struct:: wcwidth_interval_t


   An interval [start, end] (inclusive) in Unicode codepoint space.


   .. c:member:: uint32_t start

   .. c:member:: uint32_t end

.. c:function:: int wcwidth_bisearch(uint32_t ucs, const wcwidth_interval_t *table, size_t table_len)


   Binary search for \*ucs\* in a sorted interval table.

   Returns 1 if \*ucs\* is contained within any interval in \*table\*, 0 otherwise.

   \*table\* must be sorted ascending, with non-overlapping intervals.


.. c:struct:: wcwidth_override_set_t


   Terminal override record layouts.

   The six single-codepoint categories are the merged override intervals;
   grapheme clusters are stored as a flat codepoint pool with sorted lookup
   entries.


   .. c:member:: const wcwidth_interval_t *narrower

   .. c:member:: size_t narrower_len

   .. c:member:: const wcwidth_interval_t *vs16_narrower

   .. c:member:: size_t vs16_narrower_len

   .. c:member:: const wcwidth_interval_t *vs15_wider

   .. c:member:: size_t vs15_wider_len

   .. c:member:: const wcwidth_interval_t *zeroer

   .. c:member:: size_t zeroer_len

   .. c:member:: const wcwidth_interval_t *narrow_wider

   .. c:member:: size_t narrow_wider_len

   .. c:member:: const wcwidth_interval_t *narrow_zeroer

   .. c:member:: size_t narrow_zeroer_len

.. c:struct:: wcwidth_grapheme_entry_t

   .. c:member:: uint32_t offset

      into the table's grapheme codepoint pool

   .. c:member:: uint16_t len

      codepoints in the cluster

   .. c:member:: uint8_t width

      terminal-measured width

.. c:struct:: wcwidth_terminal_override_t

   .. c:member:: const char *name

      canonical terminal name

   .. c:member:: const wcwidth_override_set_t *set

   .. c:member:: const uint32_t *grapheme_pool

   .. c:member:: size_t grapheme_pool_len

   .. c:member:: const wcwidth_grapheme_entry_t *grapheme_entries

   .. c:member:: size_t grapheme_entries_len

.. c:struct:: wcwidth_terminal_alias_t

   .. c:member:: const char *alias

      TERM/TERM_PROGRAM value, lowercase

   .. c:member:: const char *canonical

      canonical terminal name


.. _SEMVER: https://semver.org
