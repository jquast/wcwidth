/*
 * Generated by wcwidth code generation (update-tables.py).
 *
 * Grapheme cluster break tables for Unicode version {{ unicode_version }}.
 */
#include "wcwidth/tables.h"

{%- for var_name, table_def in tables.items() %}
{%- set c_name = prefix ~ var_name %}

const wcwidth_interval_t {{ c_name }}[] = {
{%- for hex_start, hex_end, txt_description in table_def.hex_range_descriptions %}
    { {{ hex_start }}, {{ hex_end }} },  /* {{ txt_description }} */
{%- endfor %}
};

_Static_assert(sizeof({{ c_name }}) / sizeof({{ c_name }}[0]) == {{ c_name }}_LEN,
               "{{ c_name }}_LEN out of date");

{% endfor -%}
