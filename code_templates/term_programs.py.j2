"""
Canonical terminal names and TERM/TERM_PROGRAM aliases.

This code generated by python wcwidth project from ucs-detect data.
"""
# pylint: skip-file

KNOWN_TERMINALS = frozenset({
{%- for name in known_terminals|sort %}
    '{{ name }}',
{%- endfor %}
})

ALIASES = {
{%- for alias, canonical in aliases|dictsort %}
    '{{ alias }}': '{{ canonical }}',
{%- endfor %}
}
