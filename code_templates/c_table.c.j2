/*
 * Generated by wcwidth code generation (update-tables.py).
 *
 * Table: {{ variable_name }}
 * Unicode version: {{ table.keys()|sort|last }}
 */
#include "wcwidth/tables.h"

{%- set latest = table[table.keys()|sort|last] %}

const wcwidth_interval_t {{ c_name }}[] = {
{%- for hex_start, hex_end, txt_description in latest.hex_range_descriptions %}
    { {{ hex_start }}, {{ hex_end }} },  /* {{ txt_description }} */
{%- endfor %}
};

const size_t {{ c_name }}_LEN = sizeof({{ c_name }}) / sizeof({{ c_name }}[0]);
