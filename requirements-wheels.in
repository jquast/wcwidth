# Pinned below v3.0 to keep the manylinux2014 base image (glibc 2.17, so
# RHEL/CentOS 7 and equally old distros), and PyPy and 32-bit Linux on by
# default.  This line builds cp38-cp313; cp314 and later come from
# requirements-wheels314.in, which is free to move to a newer manylinux.
cibuildwheel==2.23.*
